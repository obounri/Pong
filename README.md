## Usage :
``` npm i ```

then

copy it from trello
```back/.env```

```
# To start the project
docker-compose up

# To rebuild images
docker-compose up --build 

# To start in detached mode 
docker-compose up -d
```
